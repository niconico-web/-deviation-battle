// client/js/mission.js

// ???????????Åò????Ä??????????????????????????????ÅL???????????????????????????
const MISSION_REWARDS = {
    XP: 300,
    COINS: 30,
};

const FINAL_REWARD_ORB_TIERS = ['tier2', 'tier3', 'tier4'];

// ?????ÅN???????????????????????Åò??????????????ÅN???????????????????????????????ÅN???????????Ä?
// ????????????????????????XP??????????????????????Ä?B??????????????????????????????????????????????????Åò???????Ä?
const MISSION_RANK_VALUES = { F: 1, E: 2, D: 3, C: 4, B: 5, A: 6, S: 7 };
function getMissionRewardForRank(rank) {
    const rankValue = MISSION_RANK_VALUES[rank] || 1;
    const reward = { xp: rankValue * 100, coins: rankValue * 50 };
    if (rankValue >= 7) reward.orbTier = 'tier4';
    else if (rankValue >= 6) reward.orbTier = 'tier3';
    else if (rankValue >= 5) reward.orbTier = 'tier2';
    return reward;
}

// Pool of possible daily missions
// ?????ÅN?????ÅN???????????????????????Åò?????????????Ä????????????????????????ÅN????????ÅN????????????????????????
// ?Ä??ÅN?????Ä??Ä??ÅL?????Ä??Ä???????????Ä??Ä????????Ä??????????????????????????????????????????????
// ?????????????????????????????????????Ä?????????????????????ÅN???????ÄÅò??????????????Åò?????????????????????????Ä?
const MISSION_POOL = [
    // --- ?????ÅN?????ÅN???????????????????????Åò?????????????????????????????? ---
    { id: 'win_bot_3', type: 'win_bot', target: 3, description: '??????????????Åò3???????????????' },
    { id: 'win_online_2', type: 'win_online', target: 2, description: '???????????????????????Åò2???????????????' },
    { id: 'study_30_min', type: 'study', target: 1800, description: '????ÅN?30???????????????' }, // 1800 seconds
    { id: 'level_up_1', type: 'level_up', target: 1, description: '????????????1?????????' },
    { id: 'create_weapon_1', type: 'create_weapon', target: 1, description: '?????ÅN???1???????????????' },
    { id: 'synthesize_orb_2', type: 'synthesize_orb', target: 2, description: '????????????2???????????????' },
    { id: 'limit_break_1', type: 'limit_break', target: 1, description: '?????ÅN???1??????????????ÅL??????' },
    { id: 'upgrade_weapon_5', type: 'upgrade_weapon', target: 5, description: '?????ÅN???5???????????????' },
    { id: 'defeat_boss_1', type: 'defeat_boss', target: 1, description: '?????????1????ÅN??????????' },

    // --- ?ÅL?????ÅL?????????Åò??????????????ÅN????????? ---
    { id: 'collect_goblin_fang_3', type: 'collect_material', target: { materialId: 'goblin_fang', count: 3 }, description: '??ÅL??????????????????3????????????', reward: getMissionRewardForRank('F') },
    { id: 'collect_slime_jelly_3', type: 'collect_material', target: { materialId: 'slime_jelly', count: 3 }, description: '????????????????????????3?Ä??????????', reward: getMissionRewardForRank('F') },
    { id: 'collect_goblin_fang_6', type: 'collect_material', target: { materialId: 'goblin_fang', count: 6 }, description: '??ÅL??????????????????6????????????', reward: getMissionRewardForRank('E') },
    { id: 'collect_goblin_hide_6', type: 'collect_material', target: { materialId: 'goblin_hide', count: 6 }, description: '??ÅL??????????????????6????????????', reward: getMissionRewardForRank('E') },
    { id: 'collect_slime_jelly_6', type: 'collect_material', target: { materialId: 'slime_jelly', count: 6 }, description: '????????????????????????6?Ä??????????', reward: getMissionRewardForRank('E') },
    { id: 'collect_slime_core_5', type: 'collect_material', target: { materialId: 'slime_core', count: 5 }, description: '?????????????????????5?Ä??????????', reward: getMissionRewardForRank('D') },
    { id: 'collect_dragon_scale_4', type: 'collect_material', target: { materialId: 'dragon_scale', count: 4 }, description: '????????ÅL????????????4????????????', reward: getMissionRewardForRank('C') },
    { id: 'collect_dragon_heart_3', type: 'collect_material', target: { materialId: 'dragon_heart', count: 3 }, description: '????????ÅL???????????????3?Ä??????????', reward: getMissionRewardForRank('C') },

    // --- ???????ÅN?????????Åò??????????????ÅN???????Ä???????????????????????????????????????? ---
    { id: 'defeat_goblin_king_easy_1', type: 'defeat_boss', target: 1, condition: { bossId: 'goblin_king', difficulty: 'easy' }, description: '??ÅL?????????????????Åã(????????????)???1????ÅN??????????', reward: getMissionRewardForRank('F') },
    { id: 'defeat_goblin_king_easy_2', type: 'defeat_boss', target: 2, condition: { bossId: 'goblin_king', difficulty: 'easy' }, description: '??ÅL?????????????????Åã(????????????)???2????ÅN??????????', reward: getMissionRewardForRank('C') },
    { id: 'defeat_goblin_king_medium_2', type: 'defeat_boss', target: 2, condition: { bossId: 'goblin_king', difficulty: 'medium' }, description: '??ÅL?????????????????Åã(????????????)???2????ÅN??????????', reward: getMissionRewardForRank('B') },
    { id: 'defeat_orc_warlord_easy_2', type: 'defeat_boss', target: 2, condition: { bossId: 'orc_warlord', difficulty: 'easy' }, description: '????????????????Åã?(????????????)???2????ÅN??????????', reward: getMissionRewardForRank('C') },
    { id: 'defeat_orc_warlord_medium_2', type: 'defeat_boss', target: 2, condition: { bossId: 'orc_warlord', difficulty: 'medium' }, description: '????????????????Åã?(????????????)???2????ÅN??????????', reward: getMissionRewardForRank('B') },
    { id: 'defeat_shadow_serpent_medium_2', type: 'defeat_boss', target: 2, condition: { bossId: 'shadow_serpent', difficulty: 'medium' }, description: '??ÅN??????????????????(????????????)???2????ÅN??????????', reward: getMissionRewardForRank('B') },
    { id: 'defeat_shadow_serpent_hard_1', type: 'defeat_boss', target: 1, condition: { bossId: 'shadow_serpent', difficulty: 'hard' }, description: '??ÅN??????????????????(?????????)???1????ÅN??????????', reward: getMissionRewardForRank('A') },
    { id: 'defeat_ice_golem_medium_2', type: 'defeat_boss', target: 2, condition: { bossId: 'ice_golem', difficulty: 'medium' }, description: '???????????ÅL?????????(????????????)???2????ÅN??????????', reward: getMissionRewardForRank('B') },
    { id: 'defeat_ice_golem_hard_1', type: 'defeat_boss', target: 1, condition: { bossId: 'ice_golem', difficulty: 'hard' }, description: '???????????ÅL?????????(?????????)???1????ÅN??????????', reward: getMissionRewardForRank('A') },
    { id: 'defeat_flame_dragon_medium_2', type: 'defeat_boss', target: 2, condition: { bossId: 'flame_dragon', difficulty: 'medium' }, description: '????????????????????ÅL???(????????????)???2????ÅN??????????', reward: getMissionRewardForRank('A') },
    { id: 'defeat_flame_dragon_hard_1', type: 'defeat_boss', target: 1, condition: { bossId: 'flame_dragon', difficulty: 'hard' }, description: '????????????????????ÅL???(?????????)???1????ÅN??????????', reward: getMissionRewardForRank('A') },
    { id: 'defeat_abyssal_knight_hard_1', type: 'defeat_boss', target: 1, condition: { bossId: 'abyssal_knight', difficulty: 'hard' }, description: '?????????????????????(?????????)???1????ÅN??????????', reward: getMissionRewardForRank('S') },
    { id: 'defeat_celestial_guardian_hard_1', type: 'defeat_boss', target: 1, condition: { bossId: 'celestial_guardian', difficulty: 'hard' }, description: '???????????????????????????????????????(?????????)???1????ÅN??????????', reward: getMissionRewardForRank('S') },
    { id: 'defeat_abyss_warden_hard_1', type: 'defeat_boss', target: 1, condition: { bossId: 'abyss_warden', difficulty: 'hard' }, description: '?????????????????????(?????????)???1????ÅN??????????', reward: getMissionRewardForRank('S') },

    // --- ??????????????Åò??????????????ÅN????????? ---
    { id: 'win_online_1_quest', type: 'win_online', target: 1, description: '???????????????????????Åò1???????????????', reward: getMissionRewardForRank('F') },
    { id: 'win_online_3_quest', type: 'win_online', target: 3, description: '???????????????????????Åò3???????????????', reward: getMissionRewardForRank('D') },
    { id: 'win_online_4_quest', type: 'win_online', target: 4, description: '???????????????????????Åò4???????????????', reward: getMissionRewardForRank('C') },
    { id: 'win_online_6_quest', type: 'win_online', target: 6, description: '???????????????????????Åò6???????????????', reward: getMissionRewardForRank('B') },

    // --- ???????????Åò??????????????ÅN????????? ---
    { id: 'study_1_hour_quest', type: 'study', target: 3600, description: '????ÅN?1??????????????????', reward: getMissionRewardForRank('E') },
    { id: 'study_2_hour_quest', type: 'study', target: 7200, description: '????ÅN?2??????????????????', reward: getMissionRewardForRank('D') },
    { id: 'study_3_hour_quest', type: 'study', target: 10800, description: '????ÅN?3??????????????????', reward: getMissionRewardForRank('C') },
];

/**
 * Get today's date as a string 'YYYY-MM-DD'
 */
function getTodayDateString() {
    const today = new Date();
    const year = today.getFullYear();
    const month = String(today.getMonth() + 1).padStart(2, '0');
    const day = String(today.getDate()).padStart(2, '0');
    return `${year}-${month}-${day}`;
}

/**
 * Initialize or reset daily missions for the player.
 */
function initializeDailyMissions() {
    let player = getPlayerData();
    if (!player) return;

    const today = getTodayDateString();
    if (!player.dailyMissions || player.dailyMissions.date !== today) {
        // Generate new missions for the day
        const shuffledMissions = [...MISSION_POOL].sort(() => 0.5 - Math.random());
        const newMissions = shuffledMissions.slice(0, 4).map(mission => ({
            ...mission,
            progress: 0,
            completed: false,
            claimed: false,
        }));

        player.dailyMissions = {
            date: today,
            missions: newMissions,
            finalRewardClaimed: false,
        };
        localStorage.setItem("player", JSON.stringify(player));
        console.log('[Mission] Daily missions initialized for', today);
    }
    renderDailyMissions();
}

/**
 * ??????????????????????????Åò??????????ÅN???Åã?Ä?????Ä????????????????????????????????Ä?
 * target??????????????Åò??????????ÅL????????????????????????ÅL??????count/seconds??????????Ä?
 */
function getMissionTargetValue(mission) {
    if (typeof mission.target === 'object' && mission.target !== null) {
        return mission.target.count || mission.target.seconds || 1;
    }
    return mission.target;
}

/**
 * ??????????????????????????Åò???????????????xp/coins/orbTier???????????????????Ä?
 * ?Ä?????????????????????ÅL?????????????????????????????????????Ä?
 */
function getMissionReward(mission) {
    if (mission.reward) return mission.reward;
    return { xp: MISSION_REWARDS.XP, coins: MISSION_REWARDS.COINS };
}

/**
 * Update progress for a specific mission type.
 * @param {string} type - The type of mission (e.g., 'win_bot', 'study').
 * @param {number|object} value - The value to add to progress or check against.
 */
function updateMissionProgress(type, value = 1) {
    let player = getPlayerData();
    if (!player || !player.dailyMissions) {
        console.log(`[Mission] Cannot update mission progress: player=${!!player}, dailyMissions=${!!player?.dailyMissions}`);
        return;
    }

    console.log(`[Mission] Updating progress for type=${type}, value=${JSON.stringify(value)}`);
    let missionsUpdated = false;
    player.dailyMissions.missions.forEach(mission => {
        if (mission.type === type && !mission.completed) {
            let progressMade = false;
            const targetValue = getMissionTargetValue(mission);
            if (type === 'defeat_boss') {
                const condition = mission.condition;
                if (condition) {
                    const bossIdMatch = !condition.bossId || condition.bossId === value.bossId;
                    const difficultyMatch = !condition.difficulty || condition.difficulty === value.difficulty;
                    if (bossIdMatch && difficultyMatch) {
                        mission.progress += 1;
                        progressMade = true;
                        console.log(`[Mission] defeat_boss progress updated: ${mission.progress}/${targetValue}`);
                    }
                } else {
                    // ?????????????????ÅL????????????????????Åò???????????????
                    mission.progress += 1;
                    progressMade = true;
                    console.log(`[Mission] defeat_boss progress updated (no condition): ${mission.progress}/${targetValue}`);
                }
            } else if (type === 'collect_material') {
                if (mission.target.materialId === value.materialId) {
                    mission.progress += value.count;
                    progressMade = true;
                }
            } else {
                mission.progress += value;
                progressMade = true;
                console.log(`[Mission] ${type} progress updated: ${mission.progress}/${targetValue}`);
            }

            if (progressMade) {
                if (mission.progress >= targetValue) {
                    mission.completed = true;
                    mission.progress = targetValue;
                    console.log(`[Mission] Mission ${mission.id} completed!`);
                }
                missionsUpdated = true;
            }
        }
    });

    if (missionsUpdated) {
        localStorage.setItem("player", JSON.stringify(player));
        renderDailyMissions();
    }
}

/**
 * Claim reward for a completed mission.
 * @param {number} missionIndex - The index of the mission to claim.
 */
function claimMissionReward(missionIndex) {
    let player = getPlayerData();
    if (!player || !player.dailyMissions) return;

    const mission = player.dailyMissions.missions[missionIndex];
    if (!mission || !mission.completed || mission.claimed) {
        alert('??????????????????????????????');
        return;
    }

    mission.claimed = true;
    const reward = getMissionReward(mission);
    player.xp = (player.xp || 0) + reward.xp;
    player.coins = (player.coins || 0) + reward.coins;

    let msg = `???????????Åò????????????\n${reward.xp} XP\n${reward.coins} ?????????`;

    // ????????????????????Åò??????B??????????????????????????????????????????????????????????????Åò??????
    if (reward.orbTier && typeof createOrb === 'function') {
        const bonusOrb = createOrb(reward.orbTier);
        if (bonusOrb) {
            if (!player.orbs) player.orbs = [];
            player.orbs.push(bonusOrb);
            msg += `\n????????????: ${typeof getOrbDisplayName === 'function' ? getOrbDisplayName(bonusOrb) : bonusOrb.tier}`;
        }
    }

    alert(msg);

    // Check for final reward
    const allClaimed = player.dailyMissions.missions.every(m => m.claimed);
    if (allClaimed && !player.dailyMissions.finalRewardClaimed) {
        player.dailyMissions.finalRewardClaimed = true;
        
        const randomTier = FINAL_REWARD_ORB_TIERS[Math.floor(Math.random() * FINAL_REWARD_ORB_TIERS.length)];
        const orb = createOrb(randomTier);
        if (orb) {
            if (!player.orbs) player.orbs = [];
            player.orbs.push(orb);
            alert(`??ÅN???????????Åò????????????\n????????ÅN?????? ${getOrbDisplayName(orb)} ????????????????????????`);
            if (typeof renderOrbInventory === 'function') renderOrbInventory();
        }
    }

    localStorage.setItem("player", JSON.stringify(player));
    updateStatus(player);
    updateXpDisplay(player);
    renderDailyMissions();
}

/**
 * Render the daily missions UI.
 */
function renderDailyMissions() {
    const container = document.getElementById('daily-missions-list');
    if (!container) return;

    const player = getPlayerData();
    if (!player || !player.dailyMissions) {
        container.innerHTML = '<p>???????????Åò???????????????????????????????Ä?</p>';
        return;
    }

    container.innerHTML = '';
    player.dailyMissions.missions.forEach((mission, index) => {
        const target = getMissionTargetValue(mission);
        const progress = Math.min(mission.progress, target);
        const progressText = mission.type === 'study' ? formatTime(progress) : `${progress} / ${target}`;
        const reward = getMissionReward(mission);
        const rewardText = `??????: ${reward.xp} XP, ${reward.coins} ?????????${reward.orbTier ? ' + ?????????' : ''}`;

        const missionEl = document.createElement('div');
        missionEl.className = 'mission-item';
        if (mission.completed) missionEl.classList.add('completed');
        if (mission.claimed) missionEl.classList.add('claimed');

        missionEl.innerHTML = `
            <div class="mission-description">${mission.description}</div>
            <div class="mission-progress">
                <div class="progress-bar-container">
                    <div class="progress-bar" style="width: ${(progress / target) * 100}%"></div>
                </div>
                <span class="progress-text">${progressText}</span>
            </div>
            <div class="mission-reward">
                <span>${rewardText}</span>
                <button class="btn btn-small claim-btn" data-index="${index}" ${!mission.completed || mission.claimed ? 'disabled' : ''}>
                    ${mission.claimed ? '?????????' : '????????????'}
                </button>
            </div>
        `;
        container.appendChild(missionEl);
    });

    // Final reward status
    const finalRewardContainer = document.getElementById('daily-final-reward');
    if (finalRewardContainer) {
        const completedCount = player.dailyMissions.missions.filter(m => m.claimed).length;
        const totalCount = player.dailyMissions.missions.length;
        finalRewardContainer.innerHTML = `
            <h4>??Ä???????????????</h4>
            <p>??ÅN?????????????????Åò????????????????????????????????????????????? (?????????: ${completedCount} / ${totalCount})</p>
            <div class="final-reward-icon">${player.dailyMissions.finalRewardClaimed ? '???' : '????'}</div>
            <p>${player.dailyMissions.finalRewardClaimed ? '??Ä??????????????????????????Åò????Ä?' : '??????: Tier2??????????????????'}</p>
        `;
    }

    // Add event listeners to claim buttons
    container.querySelectorAll('.claim-btn').forEach(btn => {
        btn.addEventListener('click', () => {
            claimMissionReward(parseInt(btn.dataset.index));
        });
    });

}
